model:
  name: neural_styler
  ver: v1
  solver: v1
  style_encoder: 'efficientnet-b0'
  k: 16

train:
  start_epoch: 0
  end_epoch: 30
  optimizer:
    mode: 'adam'
    adam:
      lr: 1e-4
      betas:
        - 0.9
        - 0.999
  scheduler:
    mode: 'StepLR'
  check_val_every_n_epoch: 1   # validate & save model every n epoch

test:
  batch_size: 1
  different_content: ~
  
data:
  name: 'coco'
  root: '../../dataset/coco/images'
  batch_size: 32
  num_workers: 32
  size: 256
  lut_root: '../datasets/luts'  # lut files from "https://www.freepresets.com/product-category/free-luts-color-lookup-table/?orderby=date"

saver:  # save model ckpt, training state
  monitor_keys:
    # key/criterion (l for low,h for high)
    - 'val/losses/total_loss-l'  # low total loss is good
  save_every_n_epoch: 10   # checkpoint auto save interval

criterion:
  lambda_consistency: 10